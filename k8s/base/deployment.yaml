apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-service
  annotations:
    sidecar.jaegertracing.io/inject: jaeger
spec:
  template:
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: video-service
        image: app:1
        env:
        - name: SENTRY_RELEASE
          value: video-service-$(VERSION)
        ports:
        - name: http
          containerPort: 8080
        envFrom:
        - secretRef:
            name: video-service
