---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: video-service
  labels:
    minikube: "true"
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: video-service.${DOMAIN}
    http:
      paths:
      - path: /
        backend:
          serviceName: video-service
          servicePort: http
