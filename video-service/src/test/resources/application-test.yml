boclips:
  teacher:
    username: teacher
    password: test

klient:
  partnerId: no-partner-id
  userId: no-user-id
  secret: no-secret
  streamFlavorParamIds: 111,222,333

event:
  monitoring:
    lookbackHours:
      search: 1
      playback: 1

email:
  host: garbage.host
  port: 466
  username: avron
  password: Champagn3

logging:
  level:
    org.hibernate.SQL: TRACE
  config: ""

jdbc:
  fetchSize: 100

spring:
  cloud:
    gcp:
      pubsub:
        enabled: false

pubsub:
  video-searchability-update:
    batch-size:
      2

batch-processing:
  video-batch-size: 10
