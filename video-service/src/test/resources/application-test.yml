boclips:
  teacher:
    username: teacher
    password: test

klient:
  partnerId: no-partner-id
  userId: no-user-id
  secret: no-secret

event:
  monitoring:
    lookbackHours:
      search: 1
      playback: 1

spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL
  data:
    mongodb:
      uri: mongodb://localhost:27017/video-service-db?ssl=false&authSource=admin

email:
  host: garbage.host
  port: 466
  username: avron
  password: Champagn3

logging:
  level:
    org.hibernate.SQL: TRACE

jdbc:
  fetchSize: 100